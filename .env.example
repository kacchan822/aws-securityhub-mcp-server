# AWS Configuration
AWS_PROFILE=default
# Primary region setting (preferred)
AWS_DEFAULT_REGION=ap-northeast-1
# Fallback if AWS_DEFAULT_REGION is not set
AWS_REGION=ap-northeast-1

# Server Configuration
LOG_LEVEL=INFO
SERVER_HOST=localhost
SERVER_PORT=8000
